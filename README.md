# Final_Project_Code

1. In order to use the data make sure to have following libaries: pandas, numpy, seaborn, matplotlib, sklearn. 

2. In order to get the data set, you must install ucimlrepo using the pip command. Remove the # on the top the first time you run the project. Make sure to fetch id = 45 to get the features.